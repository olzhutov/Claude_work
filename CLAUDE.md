# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## SETUP & COMMANDS

### Installation
```bash
pip3 install anthropic python-pptx
export ANTHROPIC_API_KEY="sk-ant-..."
export NOTION_TOKEN="..."   # Optional, for Notion integration
```

### Run the pipeline
```bash
# Recommended: folder mode
python3 agents/pipeline.py data/objects/{object_id} \
    --category income \       # "income" | "prospect"
    --doc-type memo \         # "teaser" | "memo" | "full"
    --format gamma \          # "pptx" | "gamma"
    --exchange-rate 41.5 \
    --currency USD \
    --location-score          # optional

# Legacy: single file mode
python3 agents/pipeline.py /path/to/document.pdf --category income --doc-type full --format pptx
```

### Verify setup
```bash
python3 verify_setup.py
```

### Open web calculator
Open `web_app/index.html` directly in a browser — no server needed.

### Generate a new Sakura PPTX (reference implementation)
```bash
python3 broadway_sakura.py   # outputs broadway_full.pptx
```

---

## ARCHITECTURE

```
cre_analyzer.py          ← Financial engine (NOI, Cap Rate, IRR). Pure Python stdlib.
agents/
  pipeline.py            ← CLI orchestrator. Entry point for all analysis runs.
  extractor.py           ← Reads raw docs/photos via Claude API (vision) → PropertyData JSON
  analyzer.py            ← PropertyData + cre_analyzer → AnalysisMetrics dict
  notion_publisher.py    ← → info_brief.md  (9-section brief, Ukrainian)
  memo_generator.py      ← → investment_memo.txt
  presentation_builder.py← → presentation.pptx or gamma_outline.txt
  location_scorer.py     ← Location evaluation (6 factors, property-type weights 0-10)
  folder_manager.py      ← Creates/reads object folder structure
  schemas.py             ← TypedDicts: PropertyData (60+ fields), PipelineConfig, AnalysisMetrics
  config.py              ← API keys, model name, currency defaults
web_app/index.html       ← Standalone browser calculator (HTML+CSS+JS, no build step)
broadway_sakura.py       ← CANONICAL reference for Sakura PPTX style (copy from here)
```

### Data flow
```
raw/{documents,photos,plans}/ → extractor.py → extracted/property_data.json
                                                        ↓
                                              analyzer.py → AnalysisMetrics
                                                        ↓
                              notion_publisher / memo_generator / presentation_builder
                                                        ↓
                                              output/{info_brief.md, investment_memo.txt, presentation.*}
```

### Object folder convention
```
data/objects/{object_id}/
  raw/documents/    ← PDF, DOCX, XLSX, TXT from owner
  raw/photos/       ← JPG, PNG, WEBP
  raw/plans/        ← Floor plans
  extracted/        ← property_data.json (auto-generated)
  output/           ← All final documents
```

### Key rules for new code
- **No new dependencies** — stdlib only, except `anthropic` and `python-pptx` where needed
- **All outputs in Ukrainian** — even when logic/comments are in Russian
- **Exchange rate never hardcoded** — always passed via `exchange_rate` parameter; raise error if missing when mixing currencies
- **PPTX style = Sakura** — copy components from `broadway_sakura.py`; slide size must be `10" × 5.625"`, font `Nunito Sans`, bg `#0C1828`
- **Gamma style** — accent `#D5B58A`, card bg `#122941`, body `#FCFCFC`

---

# Проект: Анализ коммерческой недвижимости

## Описание проекта

Разработка универсальных инструментов для анализа коммерческой недвижимости и земельных участков. Проект охватывает все типы объектов коммерческого назначения с приоритетом на складскую и производственную недвижимость, имущественные комплексы и офисы. Фокусируется на расчёте ключевых финансовых метрик и помощи в принятии инвестиционных решений.

---

## ПОВЕДЕНИЕ АГЕНТА-АНАЛИТИКА

### Язык
- **Общение с пользователем**: русский язык
- **Все финальные документы** (справки, меморандумы, презентации, Gamma-аутлайны): **строго украинский язык**
- Переменные и функции в коде: английский (best practice)
- Комментарии в коде, docstring'и, README: русский язык

### Главное правило — автономная работа

#### ❌ ЗАПРЕЩЕНО:
- Спрашивать пользователя "где найти информацию"
- Спрашивать "есть ли у вас данные о..."
- Останавливаться из-за нехватки данных
- Спрашивать "какой файл содержит..."
- Ждать подсказок об источниках

#### ✅ ОБЯЗАТЕЛЬНО:
- Действовать самостоятельно до получения конечного результата
- Сообщать что найдено (или не найдено) — не спрашивать где искать
- При отсутствии данных — использовать обоснованные допущения
- Помечать допущения: `⚠️ Припущення: [...]`

### Порядок поиска информации

Всегда соблюдай эту последовательность:

#### 1️⃣ Документы проекта (в первую очередь)
Самостоятельно проверь ВСЕ файлы в папке объекта. Типичный набор:

```
raw/documents/   — инвестпроект, бизнес-план, технический паспорт,
                   дозвільна документація, договори оренди, фінзвіти
raw/photos/      — фото фасадов, интерьеров, прилегающей территории
raw/plans/       — планировки, чертежи, схемы
extracted/       — уже извлечённые данные (property_data.json)
```

Читай самостоятельно: PDF, DOCX, XLSX, TXT, CSV, JPG — все форматы.

#### 2️⃣ Веб-поиск (если в документах нет)
Ищи автономно:
- **Локация**: Google Maps / OpenStreetMap — расположение, трафик, инфраструктура
- **Рынок аренды**: dom.ria.com, olx.ua, 100realty.ua — ставки, аналоги
- **Аналитика рынка**: открытые отчёты CBRE, Colliers, JLL Ukraine
- **Демография и экономика**: ukrstat.gov.ua, сайты городских советов

#### 3️⃣ Обоснованные допущения (если нигде нет)
Зафиксируй допущение и продолжай работу:
```
⚠️ Припущення: Ставку оренди прийнято на рівні $6/м²/міс на основі
   середньоринкових показників для складів класу B у містах 200-500 тис. мешканців.
```

### Начало работы с новым объектом

При получении нового объекта автоматически выполни:
1. `ls -la data/objects/{object_id}/` — найди все доступные файлы
2. Прочитай ключевые документы из `raw/`
3. Сообщи пользователю на русском: что найдено, что готов сделать
4. Приступай к работе без лишних вопросов

### Типовые сценарии

**Сценарий A: Есть полный пакет документов**
```
1. Читаю все файлы из raw/ → извлекаю данные
2. Дополняю веб-поиском (рынок, конкуренты, локация)
3. Готовлю справку / модель / презентацию на украинском
4. Помечаю что из документов, что из сети
```

**Сценарий B: Документов мало или нет**
```
1. Читаю что есть
2. Активно ищу в интернете
3. Где данных нет — фиксирую допущения с обоснованием ⚠️
4. В конце документа — список данных которые стоит уточнить
```

**Сценарий C: Пользователь задаёт вопрос по объекту**
```
1. Отвечаю сразу на русском — не спрашиваю где найти ответ
2. Указываю источник: документ / веб / допущение
3. Предлагаю углубиться в тему если нужно
```

---

## ОСНОВНЫЕ ПРАВИЛА РАЗРАБОТКИ

### 1. Технологический стек

#### Python-версия (серверная/CLI)
- **Язык**: Чистый Python (Python 3.9+)
- **Принцип**: Никаких лишних внешних библиотек
- **Стандартная библиотека**: Используются только встроенные модули Python
- **Исключения**: `anthropic`, `python-pptx`, `PyPDF2` — только когда явно нужны

#### Web-версия (браузерная)
- **Файл**: `web_app/index.html` — полностью автономный файл (HTML + CSS + JavaScript)
- **Хостинг**: Не требует сервера, открывается через `file://` или публикуется как статика
- **Зависимости**: Внешних JS/CSS-библиотек нет (чистые HTML, CSS, vanilla JavaScript)
- **Функциональность**: Дублирует логику расчётов, вычисляет NOI, Cap Rate, IRR в реальном времени
- **Поддерживаемые типы объектов в web-версии**:
  - **Склад (🏭 Warehouse)** — складские комплексы (дефолт: 5000м² GBA, $6/м²/мес, 5% вакансия, $1.2M)
  - **Офис (🏢 Office)** — офисные центры (дефолт: 8000м² GBA, $12/м²/мес, 8% вакансия, $2M)
  - **Ритейл (🛍️ Retail)** — торговые помещения (дефолт: 6000м² GBA, $15/м²/мес, 10% вакансия, $1.8M)

### 2. Контекст предметной области

#### Охват и масштаб
- **Основной охват**: ВСЕ типы коммерческой недвижимости и земельные участки
- **Поддерживаемые типы объектов**:
  - Складские комплексы (1-класс, 2-класс, 3-класс)
  - Производственные помещения и заводы
  - Имущественные комплексы
  - Офисные здания и бизнес-центры
  - Розничные помещения и торговые центры
  - Гостинично-ресторанные комплексы
  - Логистические центры
  - Земельные участки под коммерческое использование
  - Другие типы коммерческой недвижимости

#### ГЛАВНОЕ ПРАВИЛО: Универсальность
- **Принцип**: Все создаваемые инструменты, скрипты и расчёты должны быть **УНИВЕРСАЛЬНЫМИ** и работать для любого типа недвижимости
- **Реализация**:
  - Инструменты должны быть гибкими и параметризованными
  - Поддержка произвольных входных данных без привязки к типу объекта
  - Расширяемость для добавления новых категорий

### 3. Ключевые метрики (KPI) и формулы расчётов

Все расчёты реализованы в `cre_analyzer.py`. Ниже — справочные формулы.

#### 1. NOI (Net Operating Income) — Чистый операционный доход

```
PGI = GLA × rentRate × 12           # Потенциальный валовой доход
EGI = PGI × (1 - vacancyRate)       # Эффективный валовой доход
CAPEX_reserve = EGI × capexReserve% # Резерв на капитальные работы
NOI = EGI - OPEX - CAPEX_reserve    # Чистый операционный доход
```

#### 2. Cap Rate (Capitalization Rate) — Ставка капитализации

```
Cap Rate = NOI / propertyValue × 100%
```

#### 3. IRR (Internal Rate of Return) — Внутренняя норма прибыли

```
CashFlow[0] = -propertyValue
CashFlow[t] = NOI × (1 + rentGrowthRate)^(t-1)        # годы 1..N-1
CashFlow[N] = NOI_N / exitCapRate + NOI_N               # финальный год
NPV = Σ(CashFlow[t] / (1 + IRR)^t) = 0                 # метод бисекции
```

### 4. Методология разработки
- **Plan Mode**: Перед написанием любого кода ВСЕГДА предложить план
- **Утверждение**: Дождаться одобрения плана перед реализацией
- **Итерация**: Обсудить подход до начала кодирования

### 5. Региональные настройки

#### Целевая локация
- **Страна**: Украина
- **Рынок**: Коммерческая недвижимость Украины

#### Валюты расчётов
- **Поддерживаемые валюты**: Доллар США (USD, символ $) и Гривна (UAH, символ ₴)
- **Базовая валюта по умолчанию**: USD (рекомендуется для объектов дороже $100 000)

#### Правило курса валют
- Курс UAH/USD **ВСЕГДА** передаётся вручную через параметр `exchange_rate`
- **ЗАПРЕЩЕНО** хардкодить фиксированный курс в коде
- Если курс не передан при смешивании валют — вернуть ошибку
- Пример: `exchange_rate=41.5` означает 1 USD = 41.5 UAH

---

## СТРУКТУРА ПРОЕКТА

```
.
├── CLAUDE.md                       # Этот файл (правила и описание)
├── AGENTS_README.md                # Документация по системе агентов
├── cre_analyzer.py                 # Python-библиотека расчётов (универсальная)
├── web_app/
│   └── index.html                  # Веб-калькулятор (HTML + CSS + JS, автономный)
├── agents/
│   ├── __init__.py
│   ├── config.py                   # Конфигурация (API ключи, константы)
│   ├── schemas.py                  # Схемы данных (TypedDict)
│   ├── extractor.py                # Извлечение данных из документов
│   ├── analyzer.py                 # Финансовый анализ (использует cre_analyzer)
│   ├── notion_publisher.py         # Генерация информационной справки (9 разделов)
│   ├── memo_generator.py           # Генерация инвестиционного меморандума
│   ├── presentation_builder.py     # Генерация презентации (PPTX или Gamma)
│   ├── folder_manager.py           # Управление структурой папок объектов
│   └── pipeline.py                 # Главный оркестратор (CLI)
└── data/
    └── objects/
        └── {object_id}/
            ├── raw/
            │   ├── documents/      # PDF, DOCX, XLSX, TXT от собственника
            │   ├── photos/         # JPG, PNG, WEBP
            │   └── plans/          # Планировки, чертежи
            ├── extracted/          # Структурированные данные (JSON)
            └── output/             # Готовые документы (MD, TXT, PPTX)
```

---

## ВЫХОДНЫЕ ДОКУМЕНТЫ (все на украинском языке)

### 1. `info_brief.md` — Інформаційна довідка (9 розділів)
1. РЕЗЮМЕ — ключові показники
2. ЗАГАЛЬНА ІНФОРМАЦІЯ — основні параметри таблицею
3. ТЕХНІЧНІ ХАРАКТЕРИСТИКИ — адаптується під тип об'єкта
4. ЗЕМЕЛЬНА ДІЛЯНКА — кадастр, власність, термін оренди
5. СТАН ОБ'ЄКТУ ТА ІНФРАСТРУКТУРА — стан, рік, транспортність
6. РИНКОВИЙ КОНТЕКСТ — ринок, конкуренти, тренди
7. ФІНАНСОВІ ПОКАЗНИКИ — NOI, Cap Rate, сценарії
8. ОЦІНКА РИЗИКІВ — матриця ризиків
9. ДОКУМЕНТИ ТА ДОДАТКИ — перелік документів

### 2. `investment_memo.txt` — Інвестиційний меморандум
Повний текстовий документ з аналізом для інвесторів.

### 3. `presentation.pptx` або `gamma_outline.txt` — Презентація
- **pptx**: PowerPoint-файл (python-pptx), стиль Sakura
- **gamma**: Markdown-аутлайн для імпорту в gamma.app

Тип, структура та кількість слайдів визначаються протоколом опитування (див. нижче).

---

## ПРОТОКОЛ ПІДГОТОВКИ ПРЕЗЕНТАЦІЇ

Перед розробкою презентації поставити **2–3 коротких питання** з готовими варіантами відповідей.
Всі дані про об'єкт беруться з документів та інформаційної довідки — додаткових питань по об'єкту не ставити.

---

### Питання 1 — Тип презентації *(обов'язково)*

> **Який тип презентації потрібен?**
>
> **А) 🏦 Інвестиційна** — доходний об'єкт з орендарями, є фінансова модель → до 10 слайдів + галерея + контакти
> **Б) 🏢 Об'єктна** — показ об'єкта без фінансового аналізу → до 5 слайдів + галерея + контакти

---

### Питання 2 — Формат файлу *(обов'язково)*

> **В якому форматі зробити презентацію?**
>
> **А) PPTX** — PowerPoint-файл (стиль Sakura: темний фон, золоті акценти)
> **Б) Gamma** — Markdown-аутлайн для імпорту в gamma.app

---

### Питання 3 — Плани поверхів *(тільки якщо не зрозуміло з документів)*

> **Є плани поверхів з позначенням площ?**
>
> **А) Так** → додати слайд(и) планів після галереї
> **Б) Ні** → пропустити

*(Якщо плани знайдені в папці об'єкта — питання не ставити, додати автоматично)*

---

### Структура слайдів за типом

#### 🏦 Інвестиційна (до 10 основних + галерея + плани + контакти)

| # | Слайд | Що показуємо | Унікальні дані |
|---|-------|-------------|----------------|
| 1 | Обкладинка | Головне фото + 3 KPI | Ціна, GBA, Cap Rate |
| 2 | Executive Summary | Всі ключові показники | Агрегований огляд — без деталей |
| 3 | Локація | Карта + транспорт + оточення | Адреса (більше ніде) |
| 4 | Технічні параметри | GBA/GLA, поверхи, рік, стан | Всі площі (більше ніде) |
| 5 | Правовий статус | Власність, земля, дозволи | Правові деталі |
| 6 | Орендарі | Таблиця орендарів, заповненість | Ставки, строки договорів |
| 7 | Фінансова модель | NOI, Cap Rate, IRR, сценарії | Всі фін. цифри (більше ніде) |
| 8 | Ринковий контекст | Конкуренти, ринкові ставки | Порівняння з ринком |
| 9 | SWOT-аналіз | 4 квадранти | Якісна оцінка |
| 10 | Висновок | Рекомендація + міні-KPI | Підсумок без нових цифр |
| 11–N | Галерея | По 2 фото на слайді | |
| N+1 | Плани поверхів | Якщо є — план кожного поверху з площами | |
| Останній | Контакти | DE COSTA GROUP | |

#### 🏢 Об'єктна (до 5 основних + галерея + плани + контакти)

| # | Слайд | Що показуємо | Унікальні дані |
|---|-------|-------------|----------------|
| 1 | Обкладинка | Головне фото + 3 KPI | Площа, ціна/ставка, місто |
| 2 | Про об'єкт | Ключові характеристики + фото | Стан, рік, формат |
| 3 | Локація | Карта + переваги розташування | Адреса (більше ніде) |
| 4 | Технічні параметри | Площі, поверхи, інфраструктура | Всі площі (більше ніде) |
| 5 | Умови угоди | Ціна або оренда, правовий статус | Умови та контакт |
| 6–N | Галерея | По 2 фото на слайді | |
| N+1 | Плани поверхів | Якщо є — план кожного поверху з площами | |
| Останній | Контакти | DE COSTA GROUP | |

---

### Правило: без повторень

Кожне значення з'являється **рівно один раз** у найбільш релевантному слайді:

- **Адреса** → тільки слайд «Локація»
- **Площа (GBA/GLA)** → тільки слайд «Технічні параметри» (на обкладинці — одне число GBA)
- **Ціна** → обкладинка як KPI + слайд «Умови угоди» або «Висновок»
- **NOI / Cap Rate / IRR** → тільки слайд «Фінансова модель» (на обкладинці — Cap Rate як KPI)
- **Орендні ставки** → тільки слайд «Орендарі»
- **Правовий статус** → тільки слайд «Правовий статус» або «Умови угоди»
- **Контакти** → тільки останній слайд

Executive Summary — агрегований погляд, посилається на деталі, але не дублює їх.
Висновок — рекомендація та підсумок, без нових цифр яких ще не було.

---

## ФІРМОВИЙ СТИЛЬ ПРЕЗЕНТАЦІЙ — GAMMA (gamma_outline.txt)

> ⚠️ Цей розділ стосується **лише Gamma**. Для PowerPoint PPTX — див. розділ нижче.

Стиль затверджено та базується на налаштованій темі Gamma.

### Кольорова палітра (Gamma)

| Роль | HEX | Використання |
|------|-----|--------------|
| **Акцент / Золото** | `#D5B58A` | Заголовки, іконки, акцентні елементи, кнопки |
| **Фон карток** | `#122941` | Темно-синій фон карток і слайдів |
| **Текст / Тіло** | `#FCFCFC` | Основний текст на темному фоні |
| **Вторинний** | `#000000` | Додатковий акцент, тіні |
| **Межа карток** | `#565151` | Рамки, розділювачі |

### Дизайн карток (Gamma)

- **Округлість**: помірна (другий ступінь)
- **Тінь**: середня
- **Межа**: тонка лінія `#565151`
- **Прозорість**: непрозора
- **Форма зображення**: прямокутна (без обрізання)

### Застосування стилю в Gamma (gamma_outline.txt)

При генерації Gamma-аутлайну вказуй на початку файлу:

```
---
GAMMA THEME SETTINGS (застосувати вручну після імпорту):
Акцентний колір:     #D5B58A
Фон картки:          #122941
Колір тіла:          #FCFCFC
Шрифт заголовків:    Nunito Sans, Bold, Expanded
Шрифт тіла:          Nunito Sans, Regular, Expanded
Округлість карток:   помірна (2-й ступінь)
Тінь карток:         середня
Межа карток:         тонка, #565151
---
```

**Для Gamma:** после создания `gamma_outline.txt` сообщить пользователю:
> "Файл готовий. Відкрийте gamma.app → New → Import → Paste text → вставте вміст файлу gamma_outline.txt.
> Після імпорту застосуйте фірмову тему: темно-синій фон `#122941`, золоті заголовки `#D5B58A`, шрифт Nunito Sans."

---

## СТАНДАРТ ПРЕЗЕНТАЦІЙ POWERPOINT (PPTX) — «SAKURA»

> ⚠️ Цей стандарт стосується **виключно файлів .pptx** (python-pptx).
> Формат Gamma має власний стиль (розділ вище).

Стиль затверджено на базі еталонної презентації `MPZ_Sakura_Investment.pptx`.
Еталонна реалізація: `broadway_sakura.py` у корені проекту.

### Формат слайду

- **Розмір**: `10" × 5.625"` (widescreen 16:9)
- **Шрифт**: Nunito Sans (всі елементи)
- **Фон**: суцільний темно-синій `#0C1828`

### Кольорова палітра (Sakura PPTX)

| Константа | HEX | Призначення |
|-----------|-----|-------------|
| `BG`      | `#0C1828` | Фон слайду (основний) |
| `HBAR`    | `#0F1B2D` | Header bar та footer bar |
| `CARD`    | `#1A3550` | Фон темних карток та рядків |
| `GOLD_S`  | `#C9A84C` | Золота акцентна смуга (вертикальна / горизонтальна) |
| `GOLD_T`  | `#E8C97A` | Золотий текст заголовків та значень KPI |
| `WHITE`   | `#FFFFFF` | Білі картки (info_card_white) |
| `LABEL`   | `#D6E0EB` | Підписи та вторинний текст |
| `MUTED`   | `#8A9BB0` | Приглушений текст (мета-інформація) |
| `DARK_T`  | `#1A2E42` | Темний текст на білих картках |
| `GREEN`   | `#2ECC71` | Позитивний індикатор (SWOT, ризики) |
| `RED`     | `#E74C3C` | Негативний індикатор |
| `AMBER`   | `#F39C12` | Попередження / нейтральний |

### Структура слайду (постійні елементи)

Кожен слайд (крім обкладинки) містить:
1. **`gold_vbar()`** — вертикальна золота смуга зліва, `x=0, y=0, w=Pt(4), h=H` (колір `GOLD_S`)
2. **`header_bar()`** — горизонтальна панель зверху (`HBAR`, висота `Inches(0.52)`), з заголовком слайду золотом та логотипом "DE COSTA GROUP" праворуч
3. **`footer_bar()`** — горизонтальна панель знизу (`HBAR`, висота `Inches(0.32)`), з назвою об'єкта та номером слайду

Контентна зона: від `y = Inches(0.52)` до `y = H - Inches(0.32)`.

### Бібліотека компонентів

```python
# ── Утиліти ────────────────────────────────────────────────────────────────
R(slide, x, y, w, h, fill, border=None, bpt=0)      # прямокутник
T(slide, text, x, y, w, h, sz, bold, color, align,  # текстбокс
  v="ctr", wrap=False, font="Nunito Sans")
pic(slide, path, x, y, w, h)                         # фото (з fallback-заглушкою)
bg(slide)                                            # заливка фону BG

# ── Структурні компоненти ──────────────────────────────────────────────────
gold_vbar(slide)                  # вертикальна золота смуга зліва
header_bar(slide, title)          # верхня панель із заголовком
footer_bar(slide, left_txt, page) # нижня панель із назвою об'єкта та № слайду

# ── Контентні блоки ────────────────────────────────────────────────────────
kpi_card_cover(slide, x, y, w, h, value, label,      # KPI-картка обкладинки
               val_sz=16, lbl_sz=10)                 # (темний фон CARD, золото)

kpi_card_big(slide, x, y, w, h, value, label)        # великий KPI-блок
                                                     # (слайд фінансів/висновку)

info_card_white(slide, x, y, w, h, title, body,      # біла картка з заголовком
                icon_letter="i")                     # та кольоровою іконкою

stat_row(slide, x, y, w, h, label, value,            # рядок статистики
         highlight=False)                            # (темний або акцентний фон)
```

### Структура презентації (Sakura PPTX)

```
Слайд 1:   Обкладинка       — фото праворуч 35%, 3 KPI-картки знизу зліва
Слайд 2:   Executive Summary— 4 білі картки зліва + 5 stat_row праворуч
Слайд 3:   Локація           — карта зліва + 6 stat_row праворуч
Слайд 4:   Технічні параметри— 4 білі картки (GBA, GLA, поверхи, формат) + stat_row
Слайд 5:   Правовий статус  — 4 білі картки (власність, земля, оренда, дозволи) + stat_row
Слайд 6:   Орендарі         — таблиця орендарів + 5 KPI stat_row
Слайд 7:   Фінансова модель — 4 kpi_card_big + горизонтальна діаграма + таблиця сценаріїв
Слайд 8:   Ринок            — таблиця конкурентів + ринкова статистика
Слайд 9:   SWOT-аналіз      — 4 квадранти (GREEN/BLUE/AMBER/RED)
Слайд 10:  Висновок         — 5 тез + рекомендаційний блок з міні-KPI
Слайди 11–14: Фотогалерея   — по 2 фото на слайді з підписами (gallery_slide)
Слайд 15:  Контакти         — DE COSTA GROUP по центру, 4 рядки контактів
```

### KPI-картки обкладинки (slide 1)

Правило розрахунку ширини трьох KPI-карток (рівномірно на всю ліву зону):

```python
NB  = "\u00A0"          # нерозривний пробіл — запобігає переносу рядка
ML  = Inches(0.40)      # лівий відступ
MR  = Inches(0.40)      # правий відступ
GAP = Inches(0.10)      # зазор між картками
# Ширина контентної зони зліва = 6.5" (при форматі 10"×5.625", фото займає 3.5")
kw  = (Inches(6.5) - ML - MR - GAP * 2) / 3   # ≈ 1.833" на картку

# Єдиний розмір шрифту для всіх трьох значень:
max_n = max(len(v) for v, _ in kpis)
val_sz = 22 if max_n <= 5 else 18 if max_n <= 7 else 16
```

Значення KPI форматувати з нерозривними пробілами: `f"$3{NB}000{NB}000"`.

### Правила дизайну (PPTX Sakura)

**✅ Обов'язково:**
- `gold_vbar` + `header_bar` + `footer_bar` на кожному слайді (крім обкладинки)
- Обкладинка: фото займає праві 35% повної висоти, золота вертикальна лінія-роздільник
- Чергування layout: білі картки + темні рядки / 2 колонки / таблиця + KPI
- Всі KPI-значення одного набору — однаковий розмір шрифту (визначається за найдовшим)
- `wrap=False` + `\u00A0` для запобігання переносу числових значень

**❌ Заборонено:**
- Зміна розміру слайду (тільки 10" × 5.625")
- Використання інших шрифтів крім Nunito Sans
- Різний розмір шрифту для KPI-значень одного блоку
- Кольори поза затвердженою палітрою Sakura

### Файл-еталон

Повна реалізація зі всіма компонентами, всіма 15 слайдами та коментарями:
```
broadway_sakura.py   ← еталонна реалізація стандарту Sakura PPTX
broadway_full.pptx   ← згенерована презентація (вихідний файл)
```

При створенні презентації для нового об'єкта — копіювати структуру з `broadway_sakura.py`,
замінюючи дані об'єкта (назва, адреса, KPI, фото, орендарі тощо).

### 4. `property_data.json` — Извлечённые данные
Сохраняется в `extracted/`, используется для повторной обработки.

---

## ЗАПУСК PIPELINE

```bash
# Режим папки объекта (рекомендуется)
python3 agents/pipeline.py data/objects/{object_id} \
    --category income \      # "income" (доходный) или "prospect" (перспективный)
    --doc-type memo \        # "teaser" (3-5 стр), "memo" (5-10), "full" (10+)
    --format gamma \         # "pptx" или "gamma"
    --currency USD           # "USD" или "UAH"

# Пример
python3 agents/pipeline.py data/objects/warehouse_kyiv_001 \
    --category income --doc-type full --format pptx --currency USD
```

---

## СОГЛАШЕНИЯ О КОДЕ

### Стиль
- PEP 8 compliance
- Максимальная длина строки: 100 символов
- Docstring'и и комментарии — на русском языке

### Пример функции
```python
def calculate_noi(gross_revenue, operating_expenses):
    """
    Расчёт чистого операционного дохода (NOI).

    Args:
        gross_revenue: Валовой доход (в денежных единицах)
        operating_expenses: Операционные расходы

    Returns:
        float: Чистый операционный доход
    """
    # NOI = Валовой доход - Операционные расходы
    return gross_revenue - operating_expenses
```

---

## ПРОЦЕСС РАБОТЫ

1. **Задача получена** → Определяю масштаб, читаю папку объекта
2. **Анализ** → Самостоятельно извлекаю данные из документов и веба
3. **План** → Предлагаю подробный план реализации (Plan Mode)
4. **Обсуждение** → Жду одобрения или уточнений
5. **Реализация** → Пишу код / генерирую документы согласно плану
6. **Тестирование** → Проверяю работоспособность

Все вопросы, уточнения и пожелания обсуждаются на русском языке.

---

## АГЕНТ ОЦІНКИ ЛОКАЦІЇ (`agents/location_scorer.py`)

### Принцип: різні типи — різні пріоритети

Що критично для складу — не важливо для офісу. Агент використовує
**профілі ваг** залежно від типу об'єкта. Це головне правило модуля.

### Що оцінюється (6 факторів)

| Фактор | Що перевіряє агент |
|--------|--------------------|
| **Транспортна доступність** | Траси, залізниця, зупинки, метро, під'їзди для фур |
| **Демографія та ринок** | Населення міста, купівельна спроможність, ринок оренди |
| **Інфраструктура** | Електро-, газо-, водопостачання, потужності, інтернет |
| **Конкурентне середовище** | Аналоги в радіусі 5 км, рівень вакантності |
| **Потенціал розвитку** | Плани забудови, інвестиційні проєкти в районі |
| **Безпековий фактор** | Відстань від лінії фронту, частота обстрілів міста |

### LOCATION_PROFILES — профілі ваг по типах об'єктів

**Це фіксовані бізнес-правила. Не змінювати без узгодження.**

```python
LOCATION_PROFILES = {

    # СКЛАД / ЛОГІСТИКА
    # Головне: під'їзд фур, траси, залізниця. Люди не важливі.
    "склад": {
        "транспорт":      0.30,  # траси М/Н, під'їзд фур, ж/д термінал, митниця
        "інфраструктура": 0.20,  # електро (кВт), газ — для опалення/холодильників
        "ринок_праці":    0.15,  # складські робітники, охорона
        "конкуренти":     0.10,  # інші склади поряд
        "потенціал":      0.10,  # розвиток логістичного хабу
        "безпека":        0.15,  # відстань від фронту
        # Підфактори транспорту (підказки для агента):
        "_транспорт_hints": ["траса_М_або_Н", "відстань_до_КПП", "ж/д_гілка",
                             "під'їзд_фур_22т", "розв'язка_поряд"]
    },

    # ОФІС / БЦ
    # Головне: кадри, метро, престижність адреси. Траси не важливі.
    "офіс": {
        "транспорт":      0.25,  # метро/зупинки для співробітників, парковка
        "ринок_праці":    0.25,  # кваліфіковані кадри, університети поряд
        "інфраструктура": 0.10,  # стабільне електро, інтернет
        "конкуренти":     0.15,  # інші БЦ, рівень вакантності офісів
        "потенціал":      0.10,  # діловий район, що розвивається
        "безпека":        0.15,  # відстань від фронту
        "_транспорт_hints": ["метро_пішки", "маршрутки", "парковка",
                             "центр_чи_периферія", "престижність_адреси"]
    },

    # РИТЕЙЛ / ТОРГІВЛЯ
    # Головне: пішохідний трафік, видимість, покупці поряд.
    "ритейл": {
        "трафік":         0.30,  # пішохідний потік — ГОЛОВНИЙ фактор
        "демографія":     0.20,  # купівельна спроможність, щільність населення
        "транспорт":      0.15,  # зупинки, парковка для покупців
        "конкуренти":     0.15,  # якірні орендарі поряд, торгові центри
        "потенціал":      0.05,  # новобудови, зростання району
        "безпека":        0.15,  # відстань від фронту
        "_трафік_hints":  ["пішоходи_на_добу", "якірні_орендарі_поряд",
                           "видимість_з_дороги", "ЖК_поряд", "ринок_поряд"]
    },

    # ВИРОБНИЦТВО / ЗАВОД
    # Головне: потужності електро/газ, залізниця, робоча сила.
    "виробництво": {
        "інфраструктура": 0.30,  # електро (МВт!), газ, вода, каналізація — критично
        "транспорт":      0.25,  # ж/д гілка, траси для фур, відстань до порту
        "ринок_праці":    0.20,  # робітники, інженери, технічні кадри
        "конкуренти":     0.05,  # мало впливає
        "потенціал":      0.05,  # індустріальний парк, СЕЗ
        "безпека":        0.15,  # відстань від фронту — критично для виробництва
        "_інфра_hints":   ["електро_кВт_доступно", "газ_тиск", "вода_м3_добу",
                           "ж/д_гілка", "промзона_чи_ні"]
    },

    # ГОТЕЛЬНО-РЕСТОРАННИЙ КОМПЛЕКС
    "готель": {
        "трафік":         0.25,  # туристи, ділові люди
        "транспорт":      0.20,  # вокзал, аеропорт, центр
        "демографія":     0.20,  # туристичний потенціал міста
        "конкуренти":     0.15,  # інші готелі, завантаженість ринку
        "потенціал":      0.05,
        "безпека":        0.15,
        "_трафік_hints":  ["центр_міста", "вокзал_хв", "туристичні_атракції",
                           "ділові_центри_поряд"]
    },

    # БАЗОВИЙ ПРОФІЛЬ (якщо тип невідомий)
    "базовий": {
        "транспорт":      0.20,
        "демографія":     0.20,
        "інфраструктура": 0.15,
        "конкуренти":     0.15,
        "потенціал":      0.15,
        "безпека":        0.15,
    }
}
```

### Джерела даних (агент шукає автономно)

```
Транспорт:     Google Maps — відстані, маршрути, зупинки
Демографія:    ukrstat.gov.ua — населення, доходи по регіонах
Ринок оренди:  dom.ria.com, olx.ua, 100realty.ua — ставки, аналоги
Безпека:       deepstate.ua — відстань від лінії фронту
               liveuamap.com — активність обстрілів
Конкуренти:    Google Maps (пошук аналогів в радіусі 5 км)
Розвиток:      Сайти міськрад, місцеві новини — плани забудови
Інфраструктура: Сайти обленерго, міськради — потужності
```

### Формат виходу (українська мова)

```
ОЦІНКА ЛОКАЦІЇ
Адреса: [адреса]     Тип об'єкта: [тип]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Загальна оцінка:  X.X / 10  [emoji статус]

[фактор 1]:  X.X/10  — [1 речення пояснення + джерело]
[фактор 2]:  X.X/10  — [1 речення пояснення + джерело]
...

ВИСНОВОК: [2-3 речення]
СИЛЬНІ СТОРОНИ: [список]
РИЗИКИ: [список з ⚠️]
ДЖЕРЕЛА: [посилання]
```

### Порогові значення оцінки

| Бал | Статус | Рекомендація |
|-----|--------|--------------|
| 8.0–10.0 | ✅ Відмінна локація | Активно розглядати |
| 6.0–7.9  | 🟡 Приваблива локація | Розглядати з урахуванням ризиків |
| 4.0–5.9  | 🟠 Середня локація | Потрібна детальна перевірка |
| < 4.0    | 🔴 Слабка локація | Не рекомендується |

### Бонус: порівняльна таблиця

Якщо тип об'єкта не вказано явно — агент показує оцінку для **всіх типів**:

```
Та сама адреса, різні призначення:
Склад:       8.5/10  ✅
Офіс:        4.0/10  ❌
Ритейл:      2.5/10  ❌
Виробництво: 7.5/10  ✅
→ Висновок: локація оптимальна для складу або виробництва
```

### Інтеграція в pipeline

```python
# pipeline.py — додати опціональний крок
if args.location_score:
    from agents.location_scorer import score_location
    location = score_location(
        address=property_data["address"],
        property_type=property_data["property_type"]
    )
    # автоматично додається слайд "Оцінка локації" в презентацію
```

---

*Версія: 3.2 | Мова спілкування: російська | Мова документів: українська*
